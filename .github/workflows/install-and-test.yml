name: SPL TOKEN GENERATION 
run-name: ${{ github.actor }} testing SPL TOKEN GENERATION 🚀
on: [push]
jobs:
  Install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 21
      - run: npm ci
